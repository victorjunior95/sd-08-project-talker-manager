const fsTalkers = require('./talkersFile');
const validEmptyFields = require('./validEmptyFields');
const validToken = require('./validToken');
const validTalk = require('./validTalk');
const validTalker = require('./validTalker');

module.exports = {
  fsTalkers,
  validEmptyFields,
  validToken,
  validTalk,
  validTalker,
};