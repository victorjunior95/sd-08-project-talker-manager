const login = require('./login');
const token = require('./token');
const name = require('./name');
const age = require('./age');
const talk = require('./talk');
const talkValues = require('./talkValues');

module.exports = {
  login,
  token,
  name,
  age,
  talk,
  talkValues,
};
