const authorization = require('./authorization');
const name = require('./middlewareName');
const age = require('./middlewareAge');
const talk = require('./middewareTalk');
const rate = require('./middlewareRate');
const watchedAt = require('./middlewareWatchedAt');

module.exports = { authorization, name, age, talk, rate, watchedAt };
